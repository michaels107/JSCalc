/*Created 7/7/2020 by Sean Michaels
* Generation of the display with the memory inside of it. */

for(i = 0; i < memory_buttons.length; i++){

    developButton("memory_display", memory_buttons[i], "memory_display", e => {
        const display = document.getElementsByClassName("memory_display")[0];

    });

}